const state = {
  messages: []
}

const getters = {

}

const actions = {
  sendMessage (message) {
    // this._vm.$socket.emit('authenticate', localStorage.getItem('token'))
  }
}

const mutations = {

}

export default {
  state,
  getters,
  actions,
  mutations
}
